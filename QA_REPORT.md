# QA-отчёт

## Итог

- в тренажёре 115 карточек в фиксированном порядке;
- отсутствующих изображений: 0;
- повторяющихся `id`: 0;
- карточек без определённого правильного ответа: 0;
- глобальное перемешивание отключено: `shuffleTasks = false`;
- порядок букв и кнопок перемешивается один раз на попытку и сохраняется в `localStorage`;
- smoke-тест: `Smoke test passed: 115 cards`.

## Заменённые изображения

- 29 → `24.png`;
- 31 → `32.png`;
- 39 → `28.png`;
- 40 → `25.png`;
- 47 → `27.png`;
- 48 → `31.png`;
- 50 → `30.png`;
- 52 → `33.png`;
- 61 → `34.png`;
- 65 → `35.png`;
- 76 → `37.png`;
- 86 → `36.png`.

Файлы подключены как отдельные ресурсы `assets/cards/standalone-*.png`, поэтому старые кропы на этих слайдах не используются.

## Методические изменения

- `примЕрять / примИрять`: добавлены `ме́рить`, `ми́р`, `чередование мер–мир`;
- `разр_дить ружьё`: правильное `разря́д`;
- исправлено ударение `поко́рный`;
- `сокр_щать`: `кра́ткий`, `кро́ткий`, `коро́ткий`;
- слайд 67: только `отга́дка` правильна; `год`, `годный` — дистракторы; `отгадывать` отсутствует;
- устранён дубль `смя́тый`;
- `ст_чить`: `ста́ченный` / `сто́ченный`;
- слайд 86: буквы Е/И, правильная И, `ви́деть` / `ве́дать`;
- `пл_нительный`: `пле́н` / `спли́н`;
- `уп_ительный`: `по́ит`, `пой`, `пай`;
- `ут_мительный`: `то́мный` / `там`;
- `вопл_щение`: `пло́ть` / `пла́та`;
- `неисс_каемый` и `с_катор`: по три буквы Е/И/Я и указанные тройки проверочных слов;
- `ист_чать`: `исто́чник` / `ста́чивать`;
- `наваждение`: классификаторы `словарное`, `проверяемое`, `чередование`.

## Ручной ввод

Поле «Введите проверочное слово» используется на карточках:

- `нав_днение`: подводный, водный, воды, водник;
- `тайный согл_датай`: взгляд, глядя;
- `обог_щение`: богатый, богатство;
- `зак_снелый в предрассудках`: косность;
- `расс_кать волны`: рассечь, сечь, рассёк.

Поле не показывает ответ и принимает перечисленные варианты без учёта регистра; `е/ё` и знак ударения нормализуются при проверке.

## Новые карточки 112–115

1. `ут_шать малыша` — файл `20.png`;
2. `ут_шать эмоции` — файл `21.png`;
3. `зак_снелый в предрассудках` — файл `22.png`;
4. `расс_кать волны` — файл `23.png`, буквы Е/И/Я, правильная Е.

## Отображение

Ударная гласная в кнопках оборачивается в `.stressed-vowel` и выводится с размером `2em`, то есть вдвое крупнее основного текста. Новый ввод и все прежние элементы сохраняют адаптивную компоновку.

## Методическая проверка

Карточек с `needsReview: true` нет. `наваждение` оставлено отдельным словарным случаем.
